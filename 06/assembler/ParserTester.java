package assembler;

public class ParserTester {
    public static void main(String[] args) {
        // Test C-command with equals
        Parser parser = new Parser("D=M");
        
        // Test other types of commands
        // Parser parser2 = new Parser("@100");
        // Parser parser3 = new Parser("(LOOP)");
    }
}
